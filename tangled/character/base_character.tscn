[gd_scene format=3 uid="uid://q4ffjnt1tp8f"]

[ext_resource type="Texture2D" uid="uid://bf6gf5k041s7l" path="res://char_placeholder.png" id="1_aoc3k"]

[sub_resource type="GDScript" id="GDScript_aoc3k"]
script/source = "extends CharacterBody2D

# Signals
signal health_depleted
signal health_changed(new_health: float)

# Exported variables (customizable in the editor)
@export var max_health: float = 100.0
@export var speed: float = 600.0
@export var damage_rate: float = 5.0

# Internal variables
var health: float
var hurtbox: Area2D
var progress_bar: ProgressBar

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	health = max_health
	hurtbox = $HurtBox  # Ensure this node exists in your scene

# Physics process for movement and collisions
func _physics_process(delta: float) -> void:
	# Handle movement
	var direction: Vector2 = _get_input_direction()
	velocity = direction * speed
	move_and_slide()

	# Handle animations (placeholder)
	if velocity.length() > 0.0:
		pass  # Replace with: $AnimationPlayer.play(\"walk\")
	else:
		pass  # Replace with: $AnimationPlayer.play(\"idle\")

	# Handle damage from overlapping HurtBoxes
	var overlapping_bodies = hurtbox.get_overlapping_bodies()
	for body in overlapping_bodies:
		# Ensure the overlapping body is an enemy's HurtBox (not the player's own body or unrelated objects)
		if body.is_in_group(\"enemies\"):
			_take_damage(damage_rate * delta)

# Helper function to get input direction (override for enemies)
func _get_input_direction() -> Vector2:
	return Vector2(0.0,0.0)

# Damage handling
func _take_damage(amount: float) -> void:
	health -= amount
	health = max(health, 0.0)
	health_changed.emit(health)
	if progress_bar:
		progress_bar.value = health
	if health <= 0.0:
		health_depleted.emit()

# Set the ProgressBar reference (called from player/enemy scripts)
func set_progress_bar(pb: ProgressBar) -> void:
	progress_bar = pb
	progress_bar.max_value = max_health
	progress_bar.value = health
"

[sub_resource type="CircleShape2D" id="CircleShape2D_ubrwk"]
radius = 128.0

[sub_resource type="CircleShape2D" id="CircleShape2D_aoc3k"]
radius = 200.0

[node name="CharacterBody2D" type="CharacterBody2D" unique_id=1830372143]
script = SubResource("GDScript_aoc3k")

[node name="Sprite2D" type="Sprite2D" parent="." unique_id=1822690842]
texture = ExtResource("1_aoc3k")

[node name="CollisionShape2D" type="CollisionShape2D" parent="." unique_id=332495046]
z_index = 1
shape = SubResource("CircleShape2D_ubrwk")

[node name="HurtBox" type="Area2D" parent="." unique_id=1484379092]

[node name="CollisionShape2D" type="CollisionShape2D" parent="HurtBox" unique_id=1031662609]
shape = SubResource("CircleShape2D_aoc3k")
debug_color = Color(1, 0, 0, 0.33333334)
